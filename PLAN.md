# 全16単元の制作ループ

**2026-09-15 難度改訂。** 対象を偏差値50〜60に引き上げたため、旧基準で作った Unit 1〜5・7 は作り直します。難度基準は `README.md`、経緯は `REVISION-PLAN.md` にあります。

## 制作順序（改訂後）

**番号順に通します。** `master-vocab.json` は前の単元からの差分で新出語を決めるので、順番を飛ばすと語の配分が崩れます。旧 Unit 7 が45語に膨らんだのはそれが原因です。

| 順 | 内容 | 状態 |
|---|---|---|
| 1 | Unit 1 を新基準で作り直す。以降の見本になる | |
| 2〜5 | Unit 2・3・4・5 を作り直す | |
| 6 | Unit 6 を新規作成 | |
| 7 | Unit 7 を作り直す | |
| 8〜16 | Unit 8〜16 を新規作成 | |

**Unit 1 に着手する時点で `master-vocab.json` を `{"units": {}}` にリセットします。** 旧基準の累計は使えません。

旧基準のファイルは、新基準版で上書きするまで残しておいて構いません。`index.html` のリンクもそのままにします（作り直しが済むまで旧版が見えている状態のほうが、何も無いより実害が小さいため）。

---

## 0. デプロイをループに組み込む

結論から言うと、**組み込めます。ただし完全自動にするか半自動にするかで、渡すものが変わります。**

ひとつ注意点があります。PCの Cowork ワークスペースは Windows 上の隔離された Linux VM で動くため、**Windows 側の git 認証情報はそこから見えません**。つまり「接続したフォルダの中で `git push` まで走らせる」は認証で止まります。

### 方式B：半自動（推奨）

1. PCの好きな場所にリポジトリを clone する
2. そのフォルダを Cowork に接続する
3. こちらが単元ファイルを**直接そのフォルダに書き込む**
4. 最後に手元のターミナルで1行

```bash
git add -A && git commit -m "Unit 2" && git push
```

zip の受け渡しも解凍も不要。手を動かすのは最後の1行だけです。バッチファイルを1つ置いておけばダブルクリックで済みます。

### 方式A：完全自動

GitHub の **Fine-grained personal access token**（対象リポジトリを1つに限定、権限は Contents: Read and write のみ）を発行して渡してもらえれば、クラウド側から push まで通します。承認は不要になります。

対象を1リポジトリに絞れるので影響範囲は限定的ですが、トークンをチャットに貼ることにはなります。**判断はお任せします。** 迷うなら方式Bで十分です。

### 方式C：現状維持

毎回 zip を受け取ってアップロード。ループには組めません。

---

## 1. 1単元のサイクル

```
[1] ワークブック生成
      ↓
[2] python tools/check.py N --workbook     ← 機械チェック
      ↓
[3] レビュー・修正
      ↓
[4] 単語帳生成（ワークブックHTMLから語彙を機械抽出）
      ↓
[5] python tools/check.py N               ← 網羅と例文一致を照合
      ↓
[6] レビュー・修正
      ↓
[7] index.html の「準備中」をリンクに差し替え
      ↓
[8] commit & push → 公開URLで確認
```

止まるのは [3] と [6] の2回だけです。

### tools/check.py が見るもの

| 項目 | 内容 |
|---|---|
| パート指定 | 全設問に `data-part` があるか |
| パートごとの設問数 | A 3〜6／B 5〜8／C 5〜8／D 6〜10／E 4〜6／F 2〜3 |
| 設問数の合計 | 28〜36問に収まっているか |
| 解答数 | 設問数と一致するか |
| 並べ替えの語群 | 単語単位に割れているか（複合語は例外リスト） |
| 並べ替えの語数 | 選択肢の語数と正解文の語数が一致するか。`data-extra` の分は差し引く |
| 和訳 | A・C・E に `q-ja` が混ざっていないか |
| 語の網羅 | ワークブックの全語が、単語帳＋既出語＋基本語に収まっているか |
| 例文の一致 | 単語帳の例文がワークブック本文に実在するか（2択の括弧・空欄・図解の分割も吸収） |
| 見出し語の重複 | 同じ語を二重に立てていないか |
| 表示 | スマホ幅375pxで横スクロールが出ないか |
| 印刷 | A4で何ページか |
| ［参考］ | 読解本文の語数、新出語数、解説に出ていない出題語 |

［参考］の項目は NG になりません。数字を見て題材を調整するための情報です。

実行すると `tools/master-vocab.json` に既出語が積み上がります。**次の単元の「新出語」判定は、このファイルとの差分で自動的に決まります。** 単元をまたいだ重複が構造的に起きません。

元PDFの不具合は「選択肢に is が余分」「解説にない Whose を出題」といった、まさにこの機械チェックで捕まる種類のものでした。同じ穴は再発しません。

---

## 2. 1回の実行で渡すもの

16単元を1セッションで作るとコンテキストが持ちません。1回の実行では **1単元だけ**を扱います。

改訂後は1単元あたりの分量が1.3〜1.5倍になるため、1回の実行に2単元は入りません。読解本文の作成と解説の拡充で、旧基準より時間がかかります。

再開するときに渡すのは、この4つだけで足ります。

- `README.md`（仕様・品詞体系・バッジ）
- `PLAN.md`（このファイル）
- `tools/master-vocab.json`（既出語）
- 直前に作った単元のHTML1本（書きぶりの見本）

リポジトリを見れば全部そろっている状態にしてあります。

---

## 3. 単元ごとの要注意事項

作るときに必ず確認する項目です。元PDFの既知の不具合と、新規単元で必要なものをまとめてあります。

| Unit | 必ずやること |
|---|---|
| 1 | 読解は自己紹介文。肯定文しか使えないので `I am Ken. I am fifteen years old. My sister is a nurse.` の形にする。<br>着手時に `master-vocab.json` をリセットする |
| 2 | be動詞の否定文・疑問文。短縮形（isn't / aren't）を扱う |
| 3 | **新規単元。** this / that / it。複数形 these / those をどこまで出すか要判断 |
| 4 | **綴りルール表を解説に置く**（-s / -es / y→ies / f→ves）。不規則複数形も |
| 5 | 「油絵を描く」は `draw oil pictures` ではなく `paint` / `oil painting` |
| 6 | do / don't。動詞を原形のまま置くことを図解で示す |
| 7 | 単語帳に「品詞が変わる語」の一覧を置く（play / watch / clean / cook / walk / drink / help / fast / well / home）。旧版の単語帳にあるので流用してよい |
| 8 | 4格の対応表を置く。並べ替えで余分な語を入れるときは **`data-extra="1"` と問題文への明示を必ず両方**やる（旧版は明示なしで入っていてバグだった） |
| 9 | **綴りルール表を解説に置く**（-s / -es / y→ies）。解答で初出にしない |
| 10 | 解説に出さない疑問詞を出題しない（旧版は Whose がそうだった）。<br>`What is in the box?`（疑問詞が主語）を例外として独立説明 |
| 11 | **新規単元。** 時刻・曜日・日付・数。Unit 10 の疑問詞を前提にする |
| 12 | **新規単元。** 命令文・Let's 。主語が消える形なので Unit 7 の後に置いている |
| 13 | can のうしろは原形 |
| 14 | **綴りルール表を解説に置く**（子音重ね・e脱落）。<br>冒頭で「-ing形になった動詞は動詞そのものとしては働かないので be動詞と組む」と明示する（Unit 7 の「同居しない」と矛盾させない） |
| 15 | **新規単元。** was / were |
| 16 | **新規単元。** 過去形。**不規則変化の一覧表を解説に置いてから出題する** |

### 全単元で共通

- 比喩を使わない（すっぴん・ぼっち・助っ人・席・魔法・二人三脚・主役・なかみ）
- be動詞と一般動詞の判定手順は毎回同じ言い回しにする
- その単元より後で扱う文法事項を先に使わない
- 幼い表現・煽る表現を入れない
- 解答と解説は単元の末尾に含める（別冊にしない）

---

## 4. 語彙量のならし方

改訂後は **1単元25〜40語**を目安とします。下限を設けたのは、旧基準の Unit 2 が5語と極端に少なかったためです。読解本文が入ったことで、文法だけを扱う単元でも語彙を載せる場所ができました。

- 25語に届かないときは、読解本文と英作文の題材を散らして増やす
- 40語を超えるときは、問題文で使う語を絞る
- 高校入試で頻出の中1相当語を優先して入れる
- 判断材料は `tools/master-vocab.json` の累計。`check.py` を走らせるたびに更新される

文法の焦点をぼかしてまで語彙を稼ぐことはしません。あくまで全体の総量を見ながらの調整です。

## 5. まだ決めていないこと

（なし。下記はいずれも作成済み）

- ~~**基本語リスト**（`basic-words.html`）~~ 2026-09-23 作成。`check.py` の `BASIC` 77語をそのまま載せ、機能ごとに12群へ分けた。載せる範囲は「`check.py` が新出語に数えない語＝単語帳の見出し語に立てない語」と定義した
- ~~**総索引**（`word-index.html`）~~ 2026-09-23 作成。`master-vocab.json` の479語から機械生成
