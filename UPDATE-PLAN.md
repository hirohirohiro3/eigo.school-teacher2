# アップデート計画：単語テスト・総合テスト・学年拡張

2026-09-24、ひろと相談して決めた内容です。中1の16単元（ワークブック・単語帳）・総索引・基本語リストが完成したので、次の段階としてこの計画を進めます。

**このファイルの位置づけ**：`README.md`（難度基準・技術的な約束）と `PLAN.md`（中1単元の制作ループ）はそのまま有効です。この計画で変わる点は、このファイルが優先します。

- 「**決定**」はひろと合意した事項
- 「**提案**」は Claude が設計上置いたもので、変えてよい

---

## 0. 全体方針（決定）

| 項目 | 決めたこと |
|---|---|
| 長期の方針 | 中1で単語テストと総合テストができたら、**中2・中3も同じ要領で作る** |
| 使い方 | **画面で解くのが本命、印刷はおまけ。** 印刷への影響は考えなくてよい |
| JavaScript | **テストのページだけ許す。** 問題はすべて HTML に書き、JS は並べ替え・表示の切り替え・採点・結果表示・画像づくりだけに使う。ワークブック・単語帳・目次は今までどおり JS なし |
| 外部読み込み | 今までどおり Google Fonts だけ。外部ライブラリは使わない |
| リスニング | 扱わない |
| AI採点 | 使わない。**採点はすべて機械（ルール）で行う** |
| 単元番号 | **学年ごとに Unit 1 から数える** |
| 中1のファイル | **今の場所（`units/`）から動かさない。** 生徒に渡している URL を切らないため。中2は `g2/`、中3は `g3/` に置く（提案：フォルダ名） |
| 学年の切り替え | **ヘッダーに学年切り替えを入れる。** `index.html` はそのまま中1の目次として使う。実装は中2に着手するとき |
| `master-vocab.json` | **今後リセットしない。** 学年をまたいで累積する（中2で中1の語が新出語に戻らないように） |
| 部 | **今の4部のまま。** 第1部 Unit 1〜4／第2部 Unit 5〜7／第3部 Unit 8〜11／第4部 Unit 12〜16 |

---

## 1. 単語テスト（決定）

**16本のページではなく、1本のテスト画面 `word-test.html`** にする。

### 設定
- **範囲**：単元／部／中1全体（中2・中3は準備中として表示）
- **向き**：英語→日本語／日本語→英語
- **問題数**：10問／20問／全部
- 毎回ランダムな順番で出題

### 出題と採点
- **和→英**：入力して自動採点。**ヒントは出さない。**
  - 同じ和訳を持つ語は、どれを書いても正解（「見る」に see でも watch でも○）。正解時は「ほかに ○○ でも正解」と出す
  - 熟語は `〜` を除いて比べる。大文字・小文字と余分な空白は無視
  - 入力欄は自動修正・先頭の大文字化・スペルチェックを切る（綴りのテストなので）
- **英→和**：「答えを見る」→「○ わかった」「× わからない」を自分で押す

### 結果
- 点数、まちがえた語の一覧（和→英は自分の答えも）
- 「まちがえた語だけもう一度」「同じ範囲でもう一度」「範囲を選び直す」
- 途中で「やめる」を押したら、そこまでの結果を出す

### 入口
- URL の引数で範囲を選んだ状態で開ける：`word-test.html?unit=5` ／ `?part=2` ／ `?grade=1`

### データ（提案）
- 全単元の語を、ページの HTML に一覧として書く。例：`<ol id="words"><li data-g="1" data-u="16" data-en="arrive" data-ja="到着する"></li>…</ol>`
- 一覧は `units/vocabNN_*.html` の見出し語と和訳から機械で作る（手で書き写さない）
- JS は一覧を読んで、範囲で絞って並べ替えるだけ。JS が動かない端末でも一覧は読める

### 試作どおりで作る点（未確認なので変えたければ指示）
- 問題カードに「Unit 16」のような単元の表示を出す
- 単元の選択ボタンは番号だけ（単元名は選んだあと下に出る）
- 英→和の自己申告は2択

---

## 2. 総合テスト（決定）

### 本数
- **部ごとに4本**（第1部〜第4部）＋ **中1の総まとめ1本** ＝ 5本

### 形式：和歌山県公立入試に準拠
令和7・8年度の和歌山県公立入試（英語・50分・100点）は、構成も配点も2年間同じだった。**リスニングの25点を「文法の大問」25点に置き換え、ほかは入試と同じ配点にする。**

| 大問 | 内容 | 配点 | 設問の例 |
|---|---|---|---|
| 1 | **文法**（定期テスト型） | 25 | 適語選択・語形変化・並べかえ・誤文訂正 |
| 2 | 資料つきの読解（表・グラフ・予定表） | 18 | 内容の選択、資料から語や数を補う、対話の空所に3語以上で書く |
| 3 | 対話文の読解 | 21 | 流れに合う表現の選択、英問英答、下線部の内容を日本語で、内容一致 |
| 4 | 条件英作文 | 10 | 2つのうちどちらを選ぶかを理由とともに書く |
| 5 | 長文の読解（日記・スピーチ） | 26 | つなぎの語や動詞の選択、英問英答、下線部を日本語で、4文を流れに並べる、内容に合う文を2つ選ぶ |

文法の大問を置く理由：定期テストらしさに加えて、**1問1 Unit で作れるので、どの Unit が弱いかを割り出す精度を支える**（読解の問題は複数の Unit にまたがるため）。

参考にした和歌山の小問配点（令和8年度）：大問1（リスニング）2×5＋3×5 ／ 大問2 3×6 ／ 大問3 3・4・4・3・4・3 ／ 大問4 10 ／ 大問5 2×3・3×2・4・4・6。

### 中1の範囲で作るための調整
- **入試の形式を借りて、中身は中1の範囲で作る。** その部の最後の Unit までの文法だけで書く（総まとめは Unit 16 まで）。不定詞・比較・受動態・現在完了などは使わない
- 読解は入試ほど長くしない

| | 読解の題材 | 英作文の語数（提案） |
|---|---|---|
| 第1部 | 人物紹介・自己紹介 | 10語以上 |
| 第2部 | 説明文・短い対話 | 15語以上 |
| 第3部 | 予定表・時刻表などの資料（Unit 11 の数・時刻が使える） | 20語以上 |
| 第4部・総まとめ | 日記・手紙・スピーチ | 25語以上（入試と同じ） |

### 採点：すべて機械
- **正誤が機械で決まる問題**：選択・内容一致・並べかえ（語群を全部使う）・語形変化・1語の補充・文を流れに並べる・誤文訂正の「直した箇所」→ そのまま確定
- **判断が要る問題**：条件英作文・英問英答・下線部の内容を日本語で・3語以上で書く・誤文訂正の「なぜ誤りか」→ ルールで目安の点を付ける
  - 英語：語数、指定語を使ったか、文頭の大文字・文末のピリオド、**その部で扱ったよくある誤りの形**（例：`I am like`、`He play`）
  - 日本語の記述：模範解答の要点（キーワード）が入っているか
  - 画面と送る画像に「**機械の目安**」と明記する
- **「先生の採点待ち」は作らない。** 点数は機械の採点だけで出す

### 結果画面（サマリ）
- 点数（100点満点）
- 大問ごとの得点（入試と同じ見え方）
- Unit ごとの正答率を低い順に。**70%未満に「見直し」の印とワークブックへのリンク**（しきい値は提案）
- 書いた答え：英作文・記述の問題文・書いた答え・模範解答・機械チェックの結果（引っかかった箇所に色を付ける）

### 先生に送る
- 結果画面の**点数の下とページの最後**に「先生に送る（画像）」ボタン
- 押すと、端末の中で縦長の画像（幅1080px）を描いてプレビューを出す。外部ライブラリは使わず canvas に直接描く
- 「LINE などで送る」でスマホの共有画面を開く。共有画面が使えない端末は「画像を保存」
- 画像の中身：テスト名・日時・点数・大問ごとの得点・Unit ごとの正答率・**英作文と記述の問題文・書いた答え・模範解答・機械チェック**
- **文字でコピーする機能は付けない**
- 答えは生徒が送るまで端末の外に出ない

### 途中保存
- 解いている途中の答えを端末の中に一時保存し、閉じても消えないようにする（その端末だけ。どこにも送らない）

### HTML の書き方（提案）
- 各設問に `data-big`（大問）・`data-unit`（Unit、複数可）・`data-pt`（配点）を付ける
- 判断が要る問題には、模範解答・採点の目安・キーワード・誤りの形を HTML に書く
- ファイル名：`units/review01.html`〜`review04.html`（部ごと）、`units/review-final.html`（総まとめ）
- テンプレートは `tools/tpl-review.html` を新しく作り、CSS と JS（採点・結果・画像・途中保存）を入れる。`build.py` で本文と組み立てる。単元のテンプレートと同じ作り方

---

## 3. トップページ `index.html`（決定）

- 共通リンクの先頭に「**単語テスト**」（範囲を選ぶ画面へ）
- **各単元**：ボタンを3つに。ワークブック（青）／単語帳（橙）／**単語テスト**（白地に黒枠。`?unit=N` で開く）
  - テストに3つ目の色を足さないのは、青と橙がワークブックの図解で be動詞・一般動詞の色として意味を持っているため
- **各部の最後**に「第N部のまとめ」の枠：総合テスト（未作成のうちは点線の「準備中」）＋その部の単語テスト（語数つき、`?part=N`）
- **最下段**に「中1のまとめ」：**濃い地（`--ink`）に白抜き**で差別化。総まとめテスト＋中1全体の単語テスト（479語）
- 部ごとの語数：第1部 129語／第2部 95語／第3部 111語／第4部 144語

---

## 4. 制作順序（提案）

1回の実行で1つだけ扱い、`check.py` の NG が0件になるまで直してから納める。今までと同じ。

| 順 | 内容 |
|---|---|
| 1 | **単語テスト** `word-test.html` を作る。`check.py` に単語テストの検査を足す（一覧が各単語帳の見出し語・和訳と一致／漏れ・重複なし／部の定義が一致） |
| 2 | **トップページの更新**。単語テストのボタンと、各部・中1のまとめの枠（総合テストは「準備中」） |
| 3 | **総合テストの道具**：`tpl-review.html`（CSS・JS）、`build.py` の対応、`check.py` の総合テストの検査（全設問に `data-unit`・`data-pt`、配点合計100、大問の配点、Unit が部の範囲内、判断が要る問題に模範解答と目安、後で扱う文法を使っていない、読解の語数） |
| 4〜7 | **総合テスト 第1部〜第4部**（1回1本）。できたらトップページの「準備中」をリンクに差し替え |
| 8 | **中1の総まとめテスト** |
| 9 | **中2・中3の単元構成の設計**（何単元・何をどの順に。`REVISION-PLAN.md` のときと同じく、設計だけの回） |
| 以降 | 中2の単元・単語帳 → 中2のテスト → 中3…。ヘッダーの学年切り替えは中2の最初の回で入れる |

---

## 5. 残っている作業・確認事項

- **定期実行の指示文**：2026-09-24 にこの計画に合わせて書き換え済み。昼 15:00 JST と夜 1:00 JST の1日2回で、1回1工程ずつ分担して進める（夕方 20:00 の枠は停止中）
- **UI の試作3本**（単語テスト・トップページ・総合テストの結果画面）は、2026-09-24 の会話でひろに確認済み。制作時の見本として使う
- `REVISION-PLAN.md` の冒頭「まだ適用していない」は実態と違う（適用済み）。次に触るときに直す
- GitHub への反映は 09-15 以降止まっている。人が反映する
- `tpl-vocab.html` の `table.rule` の CSS が無い件は未対応のまま

---

## 6. 参考

- 和歌山県教育委員会「令和8年度和歌山県立高等学校入学者選抜学力検査問題」 https://www.pref.wakayama.lg.jp/prefg/500200/d00221870.html
- 同「令和7年度」 https://www.pref.wakayama.lg.jp/prefg/500200/d00219722.html
- 同「令和6年度」 https://www.pref.wakayama.lg.jp/prefg/500200/d00216307.html
