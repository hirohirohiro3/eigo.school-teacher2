# 制作の記録

> **2026-09-25（昼）にこのファイルを分割しました。** 09-24（夜）の記録（工程1：単語テスト `word-test.html`）と 09-24（昼）の記録は `STATUS-archive-2026-09-24.md` にそのまま入っています。それより前は `STATUS-archive-2026-09-23-night.md`、`STATUS-archive-2026-09-23.md`、`STATUS-archive-2026-09-22.md`、`STATUS-archive-2026-09.md` です。
>
> 手順は前回と同じで、**古い `STATUS.md` を `update_file` で改名し、この新しい `STATUS.md` を置いています。**アーカイブ側の中身は1バイトも触っていません。リポジトリに反映するときは、改名されたアーカイブとこの新しい `STATUS.md` の両方を置いてください。

---

## 2026-09-26　ひろの依頼で総合テストに「1問ごとの答え合わせ」を追加

定期実行の工程とは別に、会話の中で頼まれた変更です。**工程は進めていません**（次回の判定は変わらず工程5）。

### 依頼の内容

結果のサマリだけでは、どの問題が合っていてどれが間違いか分からず勉強にならない。→ **結果画面と「問題に戻る」の両方**で1問ごとの○×を見せる。先生に送る画像には**間違えた問題の番号と生徒の答え**を入れる。

### 変えたファイル

| ファイル | 種別 | 置き場所 |
|---|---|---|
| `tools/tpl-review.html` | 変更（31,588→36,631バイト） | tools |
| `units/review01.html` | 作り直し（テンプレートの変更を反映。本文は同じ。56,420→61,463バイト） | units |
| `STATUS.md` | 追記 | ルート |

### 変えたこと（`tpl-review.html`）

- **結果画面**：「Unit ごとの正答率」の下に「**1問ごとの答え合わせ**」を追加。大問ごとに全問を並べ、○（正解）×（まちがい）△（英作文・記述の一部の点）と「あなた：〜」を出し、正解でない問題には「正解：〜」も出す。選択問題は番号と選択肢の英文、並べる問題は「ア → エ → イ → ウ」の形、英作文・記述の正解は模範解答。
- **問題に戻る（見直す）**：各問の下に「○ 2 / 2点　正解」または「× 0 / 3点　あなた：〜／正解：〜」の枠を付ける。採点し直すと更新、「答えを消して」でページを読み直すと消える。
- **先生に送る画像**：「書いた答え」の前に「**まちがえた問題**」を追加。満点でない問題ごとに番号・○×△・点・生徒の答え・正解。英作文・記述は下の「書いた答え」の欄を指す（重複を避けるため）。全問正解なら「なし（全問正解）」。
- 追加は CSS（`.qa` `.qa-big` `.fb` ほか）と JS（`showAns` `rightAns` `markOf`、`grade()` の戻り値に `qa`）だけ。問題の書き方（`data-*`）は変えていないので、`build.py`・`check.py` は変更なし。

### 検証結果

- `python tools/check.py --review 1` … **18項目 NG 0件**。
- playwright（chromium）で3問に1問わざと間違えて採点（65点）→ 一覧・問題側の枠31件・送る画像（1080×5380px）の中身を目視で確認。**320 / 375 / 414 / 768px** で結果画面・問題画面とも横スクロールなし、`pageerror` 0件。

### 申し送り

- リポジトリへの反映が要るのは **`units/review01.html`・`tools/tpl-review.html`・`STATUS.md`**（このすぐ下の工程4の分 `index.html` と合わせて）。
- 第2部以降の総合テストは、このテンプレートで組めば自動で同じ答え合わせが付きます。

---

## 2026-09-26（昼）　工程4：第1部（Unit 1〜4）の総合テスト `units/review01.html`

手順5の判定は**4番目で止まりました**。1番目（`word-test.html` は Drive にあり、直前の会話での調整時に `--word-test` 11項目 NG 0件を確認済み。今回はファイルの有無で判定）、2番目（`index.html` に `word-test.html?unit=` が16件）、3番目（`tools/tpl-review.html` あり）は通過、`units/review01.html` が無かったので**工程4**です。

### 作ったもの・変えたもの

| ファイル | 種別 | 置き場所 |
|---|---|---|
| `units/review01.html` | **新規**（56,420バイト） | units |
| `index.html` | 変更（第1部のまとめの「総合テスト　準備中」を `units/review01.html` へのリンクに。16,006→16,015バイト） | ルート |
| `STATUS.md` | 追記 | ルート |

`tools/` の道具（`tpl-review.html` `build.py` `check.py`）・`word-test.html`・単元の32本・`master-vocab.json`（479語のまま）・計画書類は**変更していません**。本文は `python tools/build.py review 1 "第1部　be動詞と名詞" "Unit 1〜4" body.html` で組み立てました（本文の元ファイルは作業用なので Drive には置いていません。中身は `review01.html` の `#paper` と同じです）。

### 中身（31問・100点）

- **大問1 文法 25点**（2点×5＋3点×5）：4択5問（be動詞の形・No, I'm not.・Yes, it is.・an egg・every＋単数）／語形変化（city→cities）／並べかえ2問（Is your brother a member of the baseball club? ／ That clock over there is not old.）／誤文訂正の直した箇所2問（Yes, I'm. → I am ／ It is → They are）。1問1 Unit で Unit 1〜4 に配分。
- **大問2 資料つきの読解 18点**（3点×6）：テニス部の部員表（名前・年齢・出身・ラケットの色）とエマの紹介文（83語）。内容の選択・表から1語（Osaka／Two）・Yes/No の答えの選択・対話の空所に3語以上（No, he is not.）・内容に合う文を2つ。
- **大問3 対話文の読解 21点**（3・4・4・3・4・3）：ケンの家の美術室でのエマとケンの対話（89語）。流れに合う文の選択2問・英問英答2問・下線部 It が指すものを日本語で・内容一致。
- **大問4 条件英作文 10点**：自転車 A／B の比較表を見て、エマの妹にどちらをすすめるかを「Bike A（B）is good for your sister.」で書き始め、理由を含めて **10語以上**。
- **大問5 長文の読解 26点**（2×3・3×2・4・4・6）：転校生ミカの自己紹介スピーチ（100語）。( 1 )〜( 3 ) の語の選択（an／but／are）・英問英答2問・下線部の意味を日本語で・4文を話の順に並べる・内容に合う文を2つ。

### 検証結果

- `python tools/check.py --review 1` … **18項目 NG 0件**（読解本文 83・89・100語、範囲より後の文法 0件、375px 横スクロールなし、印刷 A4 10ページ）。
- `python tools/check.py --index` … **12項目 NG 0件**（第1部は「総合テストへのリンク」、第2〜4部と総まとめは「準備中」）。
- playwright（chromium）で **320 / 375 / 414 / 768px**：横スクロールなし・はみ出し要素0・`pageerror` 0件。
- 動作：全問に正解を入れて採点 → **100点**（25/18/21/10/26、Unit 1〜4 すべて100%）。入力後に再読み込みして**31問すべて途中保存から復元**。「先生に送る」で **1080×4045px** の PNG。大問4に Bike A を選ぶ別解（14語）を書くと 10/10、`no he not` は 0/3（要点は○、大文字・ピリオドと「he not」で減点）。

### 自分で判断したこと

1. **第1部の範囲では一般動詞・疑問詞・所有格の代名詞（his／her）・名前＋'s が使えないので、全文を be動詞の文と my／your／our だけで書きました。** 英問英答はすべて Yes/No で答える形（Is Mika from Wakayama? など）です。単元1〜4の単語帳の見出し語・基本語・人名地名以外で使った語は、表の見出しの **Age** だけです。
2. **大問4の「2つのうちどちらを選ぶか」は、好き（like）を使えないので「どちらをすすめるか」にしました。** 書き出しの文を指定し、`data-use="good"`、要点は「AかBを選んでいる」「表にある理由」の2つ。どちらを選んでも満点が取れます。
3. **`data-kw` の先頭に、要点の説明（「AかBを選んでいる」「表にある理由」「どのカメラか（机の上・古い）」）を置きました。** テンプレートは要点の1つ目の表記を結果に出すため、先頭が `cheap` だと new で書いた生徒にも「cheap が入っている」と出てしまうからです。説明の語は日本語なので答えに一致することはありません。
4. **並べる4文（大問5）は「それぞれの文が何番目か」を選ぶ形にしました**（`data-a="1,3,4,2"` はア〜エの順番）。
5. **総合テストのボタンは、単語テストと同じ白地に黒枠（`a.test`）にしました。** 見本 `ref-index.html` は今回落としていません（`--index` の検査はリンク先だけを見ます）。

### 気になった点（申し送り）

- 第1部は使える文法が be動詞だけなので、読解が単調になりやすいです。**第2部以降は一般動詞が入るので、題材の幅は広がります。**
- 判断が要る問題の要点表示の件（上の3）は、テンプレート側で「要点の説明」を別属性にするほうがきれいです。今回は道具を触らない回なので本文側で対応しました。
- 前回までの申し送り（フッターの「各30問」表記、`REVISION-PLAN.md` 冒頭、`tpl-vocab.html` の `table.rule`（**8回目**）、単語テストの別解4組）は**未対応**のままです。
- リポジトリへの反映は 09-15 以降止まったままです。**今回反映が要るのは `units/review01.html`（新規）・`index.html`・`STATUS.md` の3本。これは生徒の見え方が変わります**（トップページに第1部の総合テストが出る）。前回分（`tools/tpl-review.html`・`tools/build.py`・`tools/check.py`）がまだなら一緒に。`review01.html` はテンプレートを取り込み済みなので単体で動きますが、道具も揃えておくと次回以降の作り直しが楽です。

### 飛ばした操作

- **重複ファイルの片付けは不要でした。** ルート17件（フォルダ含む）・`tools/` 11件・`units/` 32件を全件一覧し、同名の重複はありませんでした。
- **`units/unit05`〜`unit16`（ワークブック）は落としていません。** 工程4に要らないためです。単語帳 `vocab05`〜`vocab16` は `--index` を走らせるために落としました。`word-test.html` は落とさず、判定はファイルの有無で行いました（`--word-test` は今回走らせていません）。
- **承認待ちで止まった操作はありません。**

### 次回の対象

手順5の判定では、次は**工程5：第2部（Unit 5〜7）の総合テスト**になるはずです。要るもの：`units/unit05`〜`unit07`・`units/vocab05`〜`vocab07`、`tools/tpl-review.html`、`tools/build.py`、`tools/check.py`、`tools/master-vocab.json`、`index.html`（`--index` を走らせるなら単語帳16本も）。英作文は **15語以上**、読解本文は 70〜140語。作ったら `index.html` の `id="review2"` の「準備中」をリンクに差し替えてください。

### 所要時間

開始 06:08（UTC）ごろ。検証を終え、アップロードと照合を含めておよそ1時間以内。

---

## 2026-09-26　ひろの依頼で `word-test.html` を微調整（単元ボタンから直接はじまるように）

定期実行の工程とは別に、会話の中で頼まれた調整です。**工程は進めていません。**

### 変えたこと

`word-test.html` のみ（55,649→58,416バイト）。`index.html` のリンク（`?unit=N` `?part=N` `?grade=1`）は**変えていません**。

目次のボタンから来たとき、これまでは範囲が選ばれた状態の設定画面が出て、向きと問題数を選んで「はじめる」を押す必要がありました。そこを小さなモーダル（`#launch`）に置き換え、**押した数だけ減らして**すぐ始まるようにしました。

- **`?unit=N`**：範囲は確定しているので、モーダルは**向きだけ**。問題数は**その単元の全部**に固定（`S.num="all"`）。向きのボタンを押した瞬間に開始。
- **`?part=N` / `?grade=1`**：語数が多いので、モーダルに**問題数（10/20/全部）と向き**。問題数は選ぶだけ、向きのボタンが開始を兼ねます。初期値は10問。
- **引数なし**（共通リンクの「単語テスト」）：今までどおり設定画面。モーダルは出ません。
- モーダルの一番下に「範囲から選び直す」を置き、押すと設定画面に戻れます。出題中の「やめる」も今までどおりです。

CSS は `.modal` `.m-title` `.m-sub` `.go` を追加しただけで、既存の CSS は触っていません。JS は範囲・向き・問題数の状態（`S`）を書き換えて既存の `start()` を呼ぶだけで、採点まわりには手を入れていません。

### 検証結果

- `python tools/check.py --word-test --index` … **11項目・12項目ともに NG 0件**（変更の前後で変わらず）。
- playwright（chromium）で `?unit=5` `?part=2` `?grade=1` 引数なしの4経路を確認。単元は34語全部・英→日で開始、部は20問を選んで日本語→英語で開始、「範囲から選び直す」で設定画面に戻る、引数なしは設定画面のまま、いずれも期待どおり。
- **320 / 375 / 414 / 768px** で横スクロールなし・はみ出し0・`pageerror` 0件。

### 自分で判断したこと

1. **向きのボタンを「選ぶ」と「はじめる」の兼用にしました。** 単元のときはこれで1タップ、部のときも問題数を変えなければ1タップで始まります。
2. **単元の問題数を「全部」に固定しました**（ひろの指示どおり）。設定画面から入ったときの初期値10問は変えていません。
3. **`index.html` は触っていません。** リンクの形は同じままで動くので、`--index` の検査にも影響しません。

### 申し送り

- リポジトリへの反映に **`word-test.html`（58,416バイト）**が加わります。これは生徒の見え方が変わる1本です。
- 次の定期実行（昼の枠）は、この `word-test.html` を Drive から復元したうえで**工程4**に進んでください。手順5の判定結果は変わりません。

---

## 2026-09-26（夜）　工程3：総合テストの道具（`tpl-review.html`・`build.py`・`check.py --review`）

手順5の判定は**3番目で止まりました**。1番目（`word-test.html` があり `--word-test` が11項目 NG 0件）と2番目（`index.html` に `word-test.html?unit=` が16件）は通過、3番目の `tools/tpl-review.html` が無かったので**工程3**です。4番目以降は見ていません。

### 作ったもの・変えたもの

| ファイル | 種別 | 置き場所 |
|---|---|---|
| `tools/tpl-review.html` | **新規**（31,588バイト） | tools |
| `tools/build.py` | 変更（`review` に対応。2,374→4,118バイト） | tools |
| `tools/check.py` | 変更（`--review N` を追加。30,627→44,620バイト） | tools |
| `STATUS.md` | 追記 | ルート |

`word-test.html`・`index.html`・`units/` の32本・`master-vocab.json`（479語のまま）・`README.md` `PLAN.md` `UPDATE-PLAN.md` ほかは**変更していません**。教材そのもの（`units/review*.html`）は、工程3は道具を作る回なので**作っていません**。

### `tools/tpl-review.html`（新規）

`UPDATE-PLAN.md`「2. 総合テスト」と見本 `tools/ref-review-result.html` に合わせて作りました。CSS と JS が入っていて、制作時は `<section class="big">` だけを書けば済みます。

- **CSS**：`tpl-workbook.html` と同じ `:root` の変数・手描き風の角丸（`--hand`）をそのまま使用。問題側（大問の見出し・選択肢・語群・並べかえ・入力欄・読解本文・資料の表）と、見本どおりの結果画面（点数・大問別の棒・Unit 別の正答率・書いた答え）とプレビューのモーダルを持ちます。
- **JS**（このページと `word-test.html` だけで使う約束どおり）：採点・表示の切り替え・結果表示・画像づくり・途中保存だけ。問題はすべて HTML 側にあります。外部読み込みは Google Fonts だけです。
- **設問の書き方**はテンプレート冒頭のコメントに全部書きました。`data-big` `data-name` `data-pt`（大問）、`data-unit` `data-pt` `data-type`（設問）。
- **機械で正誤が決まる種類**：`choice`（4択・ラジオ）／`multi`（2つ選ぶ・チェックボックス）／`order`（並べかえ）／`seq`（文を流れに並べる）／`fill`（補充）／`fix`（誤文訂正の直した箇所）。部分点なし。
- **ルールで目安の点を付ける種類**：`en`（条件英作文・英問英答・3語以上で書く）と `ja`（下線部の内容・なぜ誤りか）。`data-kw`（要点）があれば「要点の数の割合」で基礎点を出し、英語は語数（`data-min`）・指定語（`data-use`）・文頭の大文字・文末のピリオド・よくある誤りの形（`data-avoid`）で増減します。模範解答と完全に一致すれば満点。下限0・上限は配点。画面と送る画像に「機械の目安」と明記しています。
- **途中保存**：`localStorage` の `review:<テストのID>` に答えを入れます。その端末の中だけで、どこにも送りません。「答えを消してはじめから」で消えます。
- **先生に送る**：見本と同じく canvas に直接描いた幅1080pxの縦長 PNG。`navigator.share` が使えない端末には「画像を保存」を出します。文字でコピーする機能は付けていません。

### `tools/build.py`（変更）

`python tools/build.py review 1 "第1部　be動詞と名詞" "Unit 1〜4" body.html` で `units/review01.html` を、`review final …` で `units/review-final.html` を組み立てます。テンプレートの `{{TITLE}}` `{{NAME}}` `{{RANGE}}` `{{WTQ}}`（単語テストへのリンクの引数）`{{TEST}}`（JS が読むテストの名前と ID）`{{BODY}}` を差し替えます。ワークブック・単語帳の作り方は変えていません。

### `tools/check.py --review N`（新設・16項目）

外部読み込み／本文が入っているか／大問が5つで `data-big` が1〜5／大問の配点が 25・18・21・10・26／設問数／全設問に `data-unit`・`data-pt`・`data-type`／種類が決めたものか／配点の合計100／大問ごとの配点と設問の合計の一致／`data-unit` が部の範囲内／範囲の Unit がすべて出題に入っている／機械採点の問題の正解（選択肢の番号・2つ選ぶ・並べかえの語数と語群の一致・並べる文の数）／判断が要る問題に模範解答（`p.model`）と採点の目安（`p.rubric`）／大問4の語数条件が計画の数字と一致／読解本文の語数／**範囲より後の文法を使っていない**。

### 検証結果

- **仮の第1部（29問・100点）を作って全16項目 NG 0件**を確認しました。この仮データは**検査用なので消してあり、Drive にも上げていません**（残すと次の実行が工程4を済みと誤認するため）。
- **検査が効くことも確かめました**：配点を1点ずらす／`data-unit` を範囲外にする／模範解答を消す／範囲外の文法（can）を入れる／並べかえの語数を合わせない／大問4の語数条件を変える／読解本文を極端に短くする、のどれも NG で拾いました。戻すと0件に戻ります。
- playwright（chromium）で **320 / 375 / 414 / 768px**。いずれも**横スクロールなし・はみ出し要素0・`pageerror` 0件**。
- 動作：29問すべてに正解を入れて採点 → **100点**、大問ごとに 25/25・18/18・21/21・10/10・26/26、Unit 1〜4 がすべて100%、記述6件を表示。白紙で採点 → **0点**。途中保存は入力のたびに保存され、再読み込みで29問すべて復元。「先生に送る」で **1080×3066px** の PNG プレビューが出ました。
- `--word-test`（11項目）・`--index`（12項目）は**引き続き NG 0件**で、今回の変更の影響はありません。

### 自分で判断したこと

1. **設問の種類を8つに決めました**（`choice` `multi` `order` `seq` `fill` `fix` `en` `ja`）。`UPDATE-PLAN.md`「2.」の設問の例を、機械で採点できる形にまとめたものです。入試の「誤文訂正のなぜ誤りか」は `ja`、「3語以上で書く」は `en` に入ります。
2. **`en`・`ja` の点の出し方を上のとおり決めました。** 計画には「ルールで目安の点を付ける」としかないので、要点の一致率を基礎点にし、形式の不備を1点ずつ、よくある誤りを2点ずつ引く形にしました。**下限0・上限は配点**です。
3. **読解本文の語数に目安のレンジを置きました**（第1部 50〜110／第2部 70〜140／第3部 80〜160／第4部 90〜180／総まとめ 100〜200語）。計画には「入試ほど長くしない」としか書かれていないので、`README.md` の単元の読解（80〜120語）を基準に、部が進むほど広げました。**NG になる項目です。**工程4〜8 で窮屈なら、この数字を先に直してください。
4. **「範囲より後の文法を使っていない」は綴りで見る目安です。** Unit 3・6・8・10・12・13・14・15・16 で初めて出る形と、中1で扱わない形（不定詞・動名詞・比較・受動態・現在完了・未来）を正規表現で探します。`is interesting`（進行形に見える形容詞）や `has ten`（現在完了に見える形）は誤検出するので、「拾わない語」の一覧で外しました。**一般動詞そのものや三人称単数の -s は綴りだけでは見分けられないので見ていません。**そこは人の目と本文を書くときの注意に任せます。
5. **`check.py` の設問の拾い方を、`data-type` を持つ `<li>` だけにしました。** 選択肢も `<li>` なので、入れ子で混ざらないようにするためです。設問の `<li>` には必ず `data-type` を付けてください。
6. **`units/review01.html` は消しました。** 上のとおり検査用の仮データです。

### 気になった点（申し送り）

- 工程4〜8 で総合テストを1本作ったら、`index.html` の「準備中」をリンクに差し替えないと `--index` が NG になります（前回からの申し送り）。
- 読解本文の語数レンジ（上の3）と、範囲より後の文法の見つけ方（上の4）は、実際に本文を書いてみて窮屈／ゆるすぎなら直してください。
- フッターの「各30問」「30問」の表記のずれは**未対応**（前回の申し送り）。
- `REVISION-PLAN.md` 冒頭の「まだ適用していない」の食い違いも**未対応**。
- `tpl-vocab.html` の `table.rule` の CSS が無い件は、**7回目の申し送り**です。
- 単語テストの別解は、和訳の文字列が完全一致する4組だけ。
- リポジトリへの反映は 09-15 以降止まったままです。**今回反映が要るのは `tools/tpl-review.html`（新規）・`tools/build.py`・`tools/check.py`・`STATUS.md` の4本。**前回分（`index.html`・`tools/check.py`・`STATUS.md`・`STATUS-archive-2026-09-24.md`）とその前（`word-test.html`・`tools/gen-word-test.py`・`tools/gen-word-test.tpl.html`）もまだなら一緒に。**今回の4本は生徒の見え方を変えません**（道具だけなので、急ぎではありません）。

### 飛ばした操作

- **重複ファイルの片付けは不要でした。** ルート12件・`tools/` 10件・`units/` 32件を全件一覧し、同名の重複はありませんでした。
- **`units/unit01`〜`unit16`（ワークブック16本）は落としていません。** 工程3に要らないためです。単語帳16本は `--word-test`・`--index` を走らせるために落としました。
- **承認待ちで止まった操作はありません。**

### 次回の対象

手順5の判定では、次は**工程4：第1部（Unit 1〜4）の総合テスト**になるはずです。要るもの：`units/unit01`〜`unit04`・`units/vocab01`〜`vocab04`、`tools/tpl-review.html`、`tools/build.py`、`tools/check.py`、`index.html`。作ったら `index.html` の `id="review1"` の「準備中」をリンクに差し替えてください。

### 所要時間

開始 16:13（UTC）、検証を終えて 16:29（UTC）ごろ。アップロードと照合を含めておよそ30分。

---

## 2026-09-25（昼）　工程2：トップページ `index.html` を更新した

手順5の判定は**2番目で止まりました**。1番目（`word-test.html` があり、`python tools/check.py --word-test` が 11項目中 NG 0件）は通過、2番目は `index.html` に `word-test.html?unit=` へのリンクが0件だったので**工程2**です。3番目以降は見ていません。

### 作ったもの・変えたもの

| ファイル | 種別 | 置き場所 |
|---|---|---|
| `index.html` | 変更（12,273→16,006バイト） | ルート |
| `tools/check.py` | 変更（`--index` を追加。24,657→30,627バイト） | tools |
| `STATUS.md` | 新しく立てた（旧版は `STATUS-archive-2026-09-24.md` に改名） | ルート |

`word-test.html`、`units/` の32本、`master-vocab.json`（479語のまま）、`README.md` `PLAN.md` `UPDATE-PLAN.md` `REVISION-PLAN.md` `basic-words.html` `word-index.html`、`tools/` の残りは変更していません。

### `index.html` の変更点（`UPDATE-PLAN.md`「3.」と見本 `tools/ref-index.html` のとおり）

- 共通リンクの**先頭に「単語テスト」**（`word-test.html`）。基本語リスト・総索引はその下。
- **各単元のボタンを3つに**：ワークブック（青）／単語帳（橙）／**単語テスト**（白地に黒枠、`word-test.html?unit=N`）。
- **各部の最後に「第N部のまとめ」の枠**：総合テストは点線の「準備中」、その部の単語テスト（`?part=N`、129／95／111／144語）。
- **最下段に「中1のまとめ」**：濃い地（`--ink`）に白抜き。総まとめは「準備中」、中1全体の単語テスト（`?grade=1`、479語）。
- CSS は見本の追加分をそのまま足しました（`.u-links.three` `.u-links a.test` `.review` `.review.final`）。既存の CSS は、印刷時の `break-inside:avoid` に `.review` を足した以外は触っていません。**JS は入れていません**（`index.html` は今までどおり JS なし）。
- 後の工程で「準備中」をリンクに差し替えやすいよう、まとめの枠に `id="review1"`〜`review4`・`id="review-final"` を付けました。

### `check.py --index`（新設）

`index.html` を単語帳・単語テストの定義と突き合わせます（12項目）。script が無いこと／外部読み込みは Google Fonts だけ／共通リンクの先頭が単語テスト／3つボタンの単元カードが Unit 1〜16 の順に16件／各カードのリンク先（ワークブックは単語帳と同じスラッグ、単語テストは `?unit=N`）と単語帳の語数表示が単語帳の実際の語数と一致／各部のまとめの枠（範囲・`?part=N`・語数・総合テスト）／枠が各部の最後にある／中1のまとめが最下段で `?grade=1`・479語／濃い地。

**総合テストの扱い**：`units/reviewNN.html`（`review-final.html`）が**あればリンクになっていること、無ければ「準備中」であること**を見ます。工程4〜8で総合テストを作ったら、`index.html` の「準備中」を差し替えないとこの検査が NG になります。

### 検証結果

- `python tools/check.py --word-test --index` … **word-test 11項目・index 12項目、ともに NG 0件**。
- 検査が効くことも確かめました：`?unit=7` を `?unit=8` に、「95語」を「96語」にわざと変えると NG 2件になり、戻すと0件。
- playwright（chromium）で **320 / 375 / 414 / 768px** を確認。いずれも**横スクロールなし**、ボタンのはみ出し0、`pageerror` **0件**。320px では3つのボタンの文字が2行に折り返しますが、はみ出しはありません（見本と同じ挙動）。375px 以上では1行に収まります。
- 「第4部のまとめ」の単語テストを押すと `word-test.html?part=4` が開き、JS エラーは出ませんでした。

### 自分で判断したこと

1. **Drive からの復元は、今回は親セッションの記録（JSONL）から復号できました。** 09-24 夜の申し送りでは「親の JSONL には base64 が残らない」とありましたが、今回の環境では親の JSONL に残っていました。ただし**大きな結果（`vocab08` 38,881バイト・`word-test.html` 55,649バイト）は `…/<セッションID>/tool-results/mcp-Google_Drive-download_file_content-*.txt` に外部化**されていて、そこから復号しました。サブエージェントに `download_file_content` だけ呼ばせる方法も併用し、その分は親の JSONL・サブエージェントの JSONL・`tool-results/` のどれかにあります。**次回は3か所すべてを探す**のが確実です。復元した28本はすべて Drive の `fileSize` と一致しました。
2. **`check.py` に `--index` を足しました。** 指示の工程2には検査の追加は書かれていませんが、手順7の「NG 0件になるまで直す」を工程2でも機械で確かめられるようにするためです。工程4〜8で「準備中」の差し替え漏れも拾えます。
3. **ボタンの文言・語数の表記は今の `index.html` に合わせました。** 見本は「単語帳 33語」（半角空白）ですが、今の `index.html` は「単語帳　33語」（全角空白）なので既存のほうを残しました。
4. **`check.py` の1行が、アップロードの経路で文字の書き方だけ変わりました。** `_wt_clean` の中で全角空白を半角に置き換える箇所の、全角空白の**エスケープ表記**（バックスラッシュ＋u3000）が、アップロード時に全角空白そのものに置き換わりました（3バイト減）。Python としての意味は同じです。**Drive に置いたほうを正とし、手元もそれに合わせて検査をやり直し、NG 0件を確認しました。** `index.html` と `STATUS.md` は、アップロード後に Drive から落とし直して**バイト単位で一致**（`cmp`）を確かめています。次回以降も、アップロードする本文にバックスラッシュ＋u の表記を入れると同じことが起きるので注意してください。
5. **フッターの「各30問」はそのままにしました。** 2026-09-15 の改訂で設問数は28〜36問の幅になっており、正確ではありません（ヘッダーの「解説＋30問＋解答」も同じ）。ただし今回の工程の範囲外で、見本もそのままなので触っていません。直すかどうかは指示ください。

### 気になった点（申し送り）

- 上の「各30問」「30問」の表記のずれ（新しく気づいた点）。
- `REVISION-PLAN.md` 冒頭の「まだ適用していない」の食い違いは**未対応**のまま。
- `tpl-vocab.html` の `table.rule` の CSS が無い件は、**6回目の申し送り**です。
- 単語テストの別解は、和訳の文字列が完全一致する4組だけ（09-24 夜の申し送りのとおり）。
- リポジトリへの反映は 09-15 以降止まったままです。**今回反映が要るのは `index.html`・`tools/check.py`・`STATUS.md`（新）・`STATUS-archive-2026-09-24.md`（改名）の4本**。前回分（`word-test.html`・`tools/gen-word-test.py`・`tools/gen-word-test.tpl.html`）もまだなら一緒に。`index.html` を反映すると、生徒側から単語テストに入れるようになります。

### 飛ばした操作

- **重複ファイルの片付けは不要でした。** ルート・`tools/` を全件、`units/` は vocab 16本を一覧し、同名の重複はありませんでした（`units/` の unit 16本は一覧の先頭ページだけ確認）。
- **`units/unit01`〜`unit16`（ワークブック16本）は落としていません。** 工程2に要らないためです。`check.py --index` はワークブックのリンク先を、単語帳と同じスラッグかどうかで確かめています。
- **承認待ちで止まった操作はありません。**

### 次回の対象

手順5の判定では、次は**工程3：総合テストの道具**（`tools/tpl-review.html` が無い）になるはずです。要るもの：`tools/ref-review-result.html`、`tools/tpl-workbook.html`、`tools/build.py`、`tools/check.py`。

### 所要時間

開始 06:09（UTC）、アップロードと照合を終えて 06:22（UTC）ごろ。所要およそ15分。持ち時間4時間に対して大幅に余っていますが、1回1工程の約束どおり工程3には進みません。
